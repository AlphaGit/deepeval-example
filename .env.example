# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Specify OpenAI model (defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (defaults to INFO)
LOG_LEVEL=INFO

# Log format: "json" for JSON output, anything else for colored console (defaults to console)
# LOG_FORMAT=json

# MLflow Configuration
# Tracking URI: SQLite database or remote server URL (defaults to "sqlite:///mlflow.db")
# MLFLOW_TRACKING_URI=sqlite:///mlflow.db
